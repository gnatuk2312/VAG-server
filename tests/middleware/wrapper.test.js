describe('test wrapper', () => {
	it("Wrapper should call controller with req and res, if it gets all arguments and controller didn't call next", () => {

	});

	it("Wrapper should call controller with req and res, then call next with an error, if it gets all arguments and controller threw error", () => {

	});

	it("Wrapper should return Error, if it don't gets controller", () => {

	});

	it("Wrapper should return Error, if it don't gets req, res, next", () => {

	});
})